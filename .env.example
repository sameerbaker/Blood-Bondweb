# Blood Bond — environment configuration
# Copy this file to `.env` for local dev, or set the same values in Vercel.

# Base URL of the deployed .NET API. The frontend talks directly to this host
# (CORS is enabled on the API). No trailing slash.
VITE_API_BASE_URL=https://blood-bond.runasp.net
